import { ServiceAppointment } from './service-appointment';

describe('ServiceAppointment', () => {
  it('should create an instance', () => {
    expect(new ServiceAppointment()).toBeTruthy();
  });
});
